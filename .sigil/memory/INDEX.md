<!-- head: bd53238c8f5df59961ac64a5ff90c8bae898b024 | updated: 2026-03-21T18:16:17Z -->

# Knowledge Index

## project.md
Contains the high-level overview of what Sigil is, who it's for, and how it works conceptually. Covers the full project structure (every file in `sigil/` and `tests/`), the Python/uv/typer/litellm/PyGithub stack, all build/test/lint commands, current status (108 tests, Phase 1 MVP complete), and the business model roadmap (Phase 1 CLI tool → Phase 2 SaaS platform). Read this when you need to understand what the project does, how to run or test it, where files live, or what phase of development it's in.

## architecture.md
Contains the complete async pipeline from `sigil run` through every stage: config load, GitHub setup, knowledge staleness check, parallel analysis+ideation, validation, deduplication, parallel worktree execution, PR/issue publishing, and working memory update. Documents every component's responsibilities, the async model (litellm.acompletion, asyncio.gather, Semaphore, to_thread for PyGithub), and the full data flow diagram. Read this when you need to understand how components connect, the order of operations, why something is async, or how data transforms between pipeline stages.

## api.md
Contains the complete public API reference for every module: all dataclass definitions with field types and semantics (Finding, FeatureIdea, ExecutionResult, Config, GitHubClient, DedupResult, WorkItem), every public function signature with return types, all LLM tool schemas (write_knowledge_file, load_knowledge_files, report_finding, report_idea, validate_finding, review_idea, read_file, apply_edit, create_file, done), and all module-level constants. Read this when you need exact field names, function signatures, tool schemas, constant values, or are implementing/modifying any module's interface.

## patterns.md
Contains all coding conventions enforced in this project: Python 3.11+ type hint style, naming conventions (snake_case/PascalCase/UPPER_SNAKE_CASE/leading underscore for private), the frozen dataclass pattern with `dataclasses.replace()`, the standard LLM tool-use loop pattern (MAX_LLM_ROUNDS, message accumulation, JSON parsing, tool response format), the async subprocess pattern via `arun()`, the parallel execution pattern with Semaphore, the PyGithub async wrapping pattern, path safety validation, config loading, memory file reading, error handling philosophy, import organization, slug/branch naming, and prompt structure. Read this when writing new code, reviewing existing code for style compliance, or implementing any LLM interaction loop.

## configuration.md
Contains the full configuration schema for `.sigil/config.yml` with every field, its type, default value, and purpose. Documents all four boldness levels (conservative/balanced/bold/experimental) and exactly how each affects analysis scope and risk tolerance. Lists all focus area categories, required and optional environment variables, supported LLM model formats for Anthropic/OpenAI/Google via litellm, auto-detection logic for lint and test commands, strict validation behavior, the `.sigil/memory/` and `.sigil/ideas/` directory purposes, and the complete GitHub Actions workflow template. Read this when working on config loading/validation, adding new config fields, understanding boldness behavior, or setting up CI integration.

## execution-model.md
Contains the detailed mechanics of how Sigil executes code changes: the full worktree lifecycle (create → execute → commit → rebase → push → cleanup), the LLM tool-use code generation loop with `apply_edit` exact-match constraints, the lint→test→retry loop with error feedback, rollback on failure, the downgrade-to-issue flow with all five downgrade triggers, parallel execution with Semaphore and slug deduplication, memory conflict auto-resolution during rebase (take main's version), and the ExecutionResult interpretation table. Read this when working on executor.py, debugging why items are being downgraded, understanding branch/worktree management, or tracing why a PR failed to be created.

## knowledge-system.md
Contains the design and mechanics of Sigil's persistent knowledge system: the `.sigil/memory/` directory structure, staleness detection via HEAD SHA in INDEX.md HTML comment, the compaction flow (LLM writes files via write_knowledge_file tool), the knowledge budget formula (context_window/4 capped at 200k chars), the knowledge selection flow (LLM picks files via load_knowledge_files tool), the INDEX.md format with HEAD comment, working memory structure and update/compaction behavior, ideas storage in `.sigil/ideas/` with TTL cleanup, and the memory snapshot copy into worktrees at creation time. Read this when working on knowledge.py or memory.py, debugging stale knowledge, understanding how agents get context, or modifying the compaction/selection pipeline.

## github-integration.md
Contains the complete GitHub integration reference: token requirements and permissions, repository URL auto-detection (SSH and HTTPS formats), the deduplication system (open PRs, open issues, closed issues — all checked by normalized title), PR and issue body templates with exact markdown structure, label management (sigil label + sigil:{category} category labels with colors), rate limiting via tenacity retry decorator on 403/429, graceful degradation behavior for every failure mode (no token, no remote, auth failure, push failure, PR/issue creation failure), publishing limits enforcement, branch cleanup strategy, the GitHub Actions workflow template, and the asyncio.to_thread wrapping pattern for all PyGithub calls. Read this when working on github.py, debugging PR/issue creation failures, understanding deduplication logic, or setting up GitHub Actions.

## dependencies.md
Contains the full dependency manifest: runtime deps (typer, rich, litellm, PyGithub, tenacity, pyyaml) with versions and exact purposes, dev deps (pytest, pytest-asyncio, ruff) with configuration notes, the internal module dependency graph showing which modules import which, external service dependencies (LLM API, GitHub API, git binary), supported litellm model string formats, and the list of removed dependencies (tree-sitter-languages removed in issue #024, threading removed in issue #022). Read this when adding or updating dependencies, understanding why a specific package is used, tracing import relationships between modules, or investigating why a removed dependency should stay removed.

## testing.md
Contains the complete testing reference: framework setup (pytest-asyncio with asyncio_mode=auto), full directory structure of all test files, test style conventions (plain functions, descriptive names, parametrize, tmp_path/monkeypatch fixtures), the standard async test pattern with LLM mocking, the LLM tool-call response mock pattern (MagicMock chain for choices/message/tool_calls), GitHub client mock pattern, real git repo initialization pattern for executor tests, factory functions (_make_finding, _make_idea), and a detailed per-module coverage breakdown listing every test case by name. Read this when writing new tests, understanding how to mock LLM responses or GitHub calls, finding which test covers a specific behavior, or debugging test failures.

## working.md
Contains Sigil's operational working memory from previous runs: a summary of what was found and done in the most recent run (8 findings, 8 ideas, dry-run mode), key findings about documentation gaps (missing README, LICENSE, CONTRIBUTING.md, py.typed), missing pyproject.toml metadata, empty integration tests, and enhancement ideas (config wizard, rich UI, plugin system, multi-repo ops). Also records patterns learned about the repo and prioritized focus areas for the next run. Read this when you need to know what Sigil has already attempted, what was found in previous analysis, what to avoid re-surfacing, or what the highest-priority next actions are.
